# Insta v2.0 - System Dependencies
# Install these packages before running Brute.sh

# Core dependencies
tor
curl
openssl

# Standard POSIX utilities (usually pre-installed)
# awk
# sed
# cat
# tr
# wc
# cut
# uniq

# Installation (Debian/Ubuntu/Kali):
# sudo apt-get install -y tor curl openssl

# Installation (Arch Linux):
# sudo pacman -S tor curl openssl

# Installation (Fedora/RHEL):
# sudo dnf install -y tor curl openssl
